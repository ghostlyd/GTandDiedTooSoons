# Inventory

This folder stores non-sensitive snapshots of the local Ableton/Arturia production environment.

Run:

```bash
python3 scripts/inventory_live_suite.py
```

The script records installed application names, Ableton Factory Pack names, high-level file counts, and home-relative paths. It does not copy packs, plugins, samples, crash reports, licenses, or secrets.
