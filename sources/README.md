# Sources

Raw source audio is intentionally ignored by Git. Use `catalogs/public-domain-bluegrass-sources.json` for provenance and `scripts/fetch_public_domain_audio.py` for approved downloads.

Downloads land under `sources/public-domain/raw/` with local provenance sidecars. Commit metadata and process changes, not bulky or uncleared audio.
